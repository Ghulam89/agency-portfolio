
const styles = {
    Container: {
        py: "5em",
        position: "relative",
        backgroundImage: theme => `${theme.palette.primary.ContactGradient}, url("${'/avatars/contact.png'}")`
    },
};

export default styles;